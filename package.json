{
  "name": "edg-eth-bridge",
  "scripts": {
    "deploy": "tsc && node index.js deployBridge && cd relayer && make build",
    "build": "tsc"
  },
  "main": "index.js",
  "bin": {
    "edg-eth-bridge": "index.js"
  },
  "dependencies": {
    "@types/node": "^14.14.37",
    "commander": "^7.2.0",
    "dotenv": "^8.2.0",
    "ethers": "^5.1.0"
  }
}
